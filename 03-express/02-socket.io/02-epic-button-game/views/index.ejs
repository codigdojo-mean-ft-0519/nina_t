<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <link rel="stylesheet" href="/styles.css">
    <title>The Epic Button Game</title>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.3.1/jquery.min.js"></script>
    <script type="text/javascript" src="/socket.io/socket.io.js"></script>
    <script type ="text/javascript">
        $(document).ready(function (){
            var socket = io.connect();
            $(".epic").click(function(){
                socket.emit("counting")
                });
            socket.on('new_number', function (data) { //4
                $("#span").html(data.count);
            });
            $(".destroy").click(function(){
                socket.emit("destroy");
            });
         })
    </script>

</head>
<body>
    <div class="container">
        <h1>The button has been pushed <span id="span">0</span> time(s)</h1>
        <p>Push the button to update the count!</p>
        <button class="epic">Push the epic button</button>
        <button class="destroy">Reset count</button>
    </div>
    
</body>
</html>